diff --git a/package/lean/luci-app-frpc/root/etc/config/frp b/package/lean/luci-app-frpc/root/etc/config/frp
index e3508fe..323b6a6 100644
--- a/package/lean/luci-app-frpc/root/etc/config/frp
+++ b/package/lean/luci-app-frpc/root/etc/config/frp
@@ -5,13 +5,61 @@ config frp 'common'
 	option enable_cpool '0'
 	option time '40'
 	option tcp_mux '1'
-	option enabled '0'
-	option vhost_http_port '80'
-	option vhost_https_port '443'	
-	option server_addr 'yourdomain.com'
 	option server_port '7000'
-	option token '1234567'
 	option log_level 'info'
 	option enable_http_proxy '0'
 	option protocol 'tcp'
+	option server_addr 'frp.freefrp.net'
+	option token 'freefrp.net'
+	option vhost_http_port '32380'
+	option vhost_https_port '32443'
+	option enabled '1'
+
+config proxy
+	option enable '1'
+	option type 'http'
+	option domain_type 'subdomain'
+	option local_ip '10.3.1.3'
+	option local_port '80'
+	option proxy_protocol_version 'disable'
+	option use_encryption '1'
+	option use_compression '1'
+	option subdomain 'x86-lede-web'
+	option remark 'x86-lede-web'
+
+config proxy
+	option enable '1'
+	option type 'tcp'
+	option remote_port '32322'
+	option local_ip '10.3.1.3'
+	option local_port '22'
+	option proxy_protocol_version 'disable'
+	option use_encryption '1'
+	option use_compression '1'
+	option remark 'x86-lede-ssh'
+
+config proxy
+	option enable '1'
+	option type 'http'
+	option domain_type 'subdomain'
+	option proxy_protocol_version 'disable'
+	option use_encryption '1'
+	option use_compression '1'
+	option subdomain 'x86-lede-jd'
+	option local_ip '10.3.1.3'
+	option local_port '5678'
+	option remark 'x86-lede-jd'
+
+config proxy
+	option enable '1'
+	option type 'http'
+	option domain_type 'subdomain'
+	option subdomain 'x86-lede-portainer'
+	option local_ip '10.3.1.3'
+	option local_port '9000'
+	option proxy_protocol_version 'disable'
+	option use_encryption '1'
+	option use_compression '1'
+	option remark 'x86-lede-portainer'
+
